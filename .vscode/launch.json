{
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "API: Attach",
            "port": 9229,
            "restart": true,
            "cwd": "${workspaceFolder}/apps/api",
            "skipFiles": ["<node_internals>/**"]
        }
    ]
}
